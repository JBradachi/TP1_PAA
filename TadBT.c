#include "./headers/TadBT.h"

int RecursãoBT(Mapa* mapa, int posX, int posY, int q, int v[4], int h[4]){
    int xn, yn, q1, i;
    for(i=0; i<4 i++){
        q1 = 0;
        xn = posX + h[i];
        yn = posY + h[i];
        if(xn < mapa->coluna && yn < mapa->linha)
    }
}